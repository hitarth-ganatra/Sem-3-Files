// IT NEVER STOP
// NEVER IT STOP

// Swap first and second word from the string

#include<iostream>
#include<string.h>
using namespace std;

int main()
{
    char sentence[100];
    char words[100][100];
    int count = 0;

    cout << "Enter a sentence : ";
    cin.getline(sentence, 100);

    for (int i = 0; i < strlen(sentence); i++)
    {
        if (sentence[i] == ' ')
        {
            
        }
    }
    cout << count;
    cout << sentence;

    return 0;
}