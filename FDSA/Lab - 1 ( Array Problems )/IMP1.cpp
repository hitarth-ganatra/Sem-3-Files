// Take random numbers from 1 to 20 and it must be odd, if not reject it. Total 8 numbers should be extracted and it must be totalled to 20.
// Another thing to note is that number of elements should not be same, so if in first series there are 3 1s, then in another series not the same numbers are to be there in second and all other series
// In total 8 series are to be found.

#include<iostream>
using namespace std;

int main()
{
    cout << rand() ;
    return 0;
}